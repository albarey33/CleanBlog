I"Ò<h2 id="01-merging-excel-files-with-equal-structure-with-inheritr">01 Merging Excel files with equal structure with inherit.R</h2>
<p>Script to read excel files with the same structure of fields.</p>

<p>Use Case: To import data on parts and integrate them into one data file</p>

<p>Example: To build the monthly enrollment list data CSV file for data analysis of Care Management results.</p>

<p>Data Source: PATIENT HEALTH DASHBOARD (PHD) - POPULATION SUMMARY</p>

<p>FREQUENCY OF PROCESS: Execute this script after enrollment ONCE A MONTH</p>

<p><a href="https://github.com/albarey33/Data_Analysis_R/blob/main/01%20Merging%20Excel%20files%20with%20equal%20structure%20with%20inherit.R">R Script - Merge Excel Files</a></p>

<h1 id="brief-description">BRIEF DESCRIPTION:</h1>

<h2 id="required-packages">Required Packages</h2>
<p>dplyr, readxl, data.table</p>

<h3 id="create-a-list-of-the-excel-files-names">Create a list of the Excel files names</h3>
<p>Use of list.files (base) to take out the names, use regex to define a pattern of needed files.</p>

<h3 id="create-a-function-to-read-the-data-inside-the-excel-files-create-tibbles">Create a function to read the data inside the Excel files. Create Tibbles</h3>
<p>Use readxl::read_excel(filename), along with nrow and length to get the data dimensions. 
For convenience, the fx also converts the logical fields to character.</p>

<h3 id="merge-the-tibbles-into-one-data-file">Merge the tibbles into one data file</h3>
<p>Using do.call bind_rows</p>

<h3 id="change-field-names">Change field names</h3>
<p>By default the names are prefixed with X00. Delete these using regex.</p>

<h3 id="write-the-results-into-a-csv">Write the results into a CSV</h3>
<p>Using datatable::fwrite()</p>

:ET